'use client';

// This page is intentionally empty.
// The AdminShell in layout.tsx handles redirecting authenticated users
// to their first permitted module after session check completes.
export default function AdminPage() {
  return null;
}
